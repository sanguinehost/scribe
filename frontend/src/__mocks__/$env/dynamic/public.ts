// frontend/src/__mocks__/$env/dynamic/public.ts

// Mock the public dynamic environment variables
// Add any PUBLIC_ environment variables your code might expect during tests.
// For example, if your constants.ts uses env.PUBLIC_API_URL:
// export const env = {
//   PUBLIC_API_URL: 'http://localhost:3000/mock-api',
// };

// For now, an empty object should suffice if no specific vars are immediately needed for the test to pass this import.
export const env = {}; 