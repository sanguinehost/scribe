[package]
name = "scribe-config"
version = "0.1.0"
edition = "2021"

[features]
payment = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
envy = "0.4"
dotenvy = "0.15"
anyhow = "1.0"
tracing = "0.1"
url = "2.5"
serde_json = "1.0"
validator = { version = "0.20", features = ["derive"] }

[dev-dependencies]
proptest = "1.6"
